//2) Criar um ARRAY de números e use MAP para dobrar os valores.

const numeros = [1,2,3,4,5];
const numDobrados = numeros.map((numero) => numero*2);
console.log("Numeros originais: ",numeros);
console.log("Numeros dobrados: ",numDobrados);
